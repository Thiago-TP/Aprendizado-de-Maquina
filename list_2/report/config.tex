% Report's condifguration and formatting file.
% All packages should be loaded here, all commands should be defined here.

\usepackage[margin=3cm]{geometry}

\usepackage[scaled]{helvet}

\usepackage{indentfirst}

\usepackage{enumitem}
    \setlist{itemsep=0pt}

\usepackage{amsmath}
    \newcommand{\argmin}{\mathop{\mathrm{argmin}}\limits}
    \newcommand{\argmax}{\mathop{\mathrm{argmax}}\limits}
\usepackage{amssymb}
\usepackage{bm}

\usepackage{titlesec}
    \titleformat{\section}{\sffamily\bfseries}{$\blacktriangleright$}{1em}{}
    \titleformat{\subsection}{\sffamily\bfseries}{$\triangleright$}{1em}{}


\usepackage{graphicx}
    \graphicspath{{figures/}}

\usepackage{caption, subcaption}

\usepackage{fancyhdr}
    \fancyhead[l]{Thiago Tomás de Paula}
    \fancyhead[c]{242104677}
    \fancyhead[r]{Assignment 2}

    \usepackage{lastpage}
    \fancyfoot[l]{\today}
    \fancyfoot[c]{Aprendizado de Máquina}
    \fancyfoot[r]{p. \thepage/\pageref*{LastPage}}

    \pagestyle{fancy}

\usepackage[ruled]{algorithm2e}
    \SetKwComment{Comment}{$\triangleright$ }{}

\usepackage{tikz}
    \usetikzlibrary{shapes, positioning}
    % Flowchart blocks
    \tikzstyle{startstop} = [
        rectangle, 
        rounded corners, 
        minimum width=2cm, 
        minimum height=1cm,
        text centered, 
        draw=black, 
        fill=red!40,
    ]

    \definecolor{brightblue}{HTML}{23C5FB} 
    \tikzstyle{process} = [
        rectangle, 
        minimum width=2cm, 
        minimum height=1cm, 
        text centered, 
        draw=black, 
        fill=brightblue!30,
        trapezium stretches=true,
    ]
    \tikzstyle{io} = [
        trapezium, 
        trapezium left angle=70, 
        trapezium right angle=110, 
        minimum width=2cm, 
        minimum height=1cm, 
        text centered, 
        draw=black, 
        fill=yellow!40,
        trapezium stretches=true,
    ]
    \tikzstyle{decision} = [
        diamond, 
        minimum width=1cm, 
        minimum height=1cm, 
        text centered, 
        draw=black, 
        fill=teal!20,
    ]

    \newcommand{\drawClusterOne}[1]{
        \draw[fill=#1]
            (-0.1, 1.4) circle (\r)
            (0.1, 2) circle (\r)
            (-0.2, 2.5) circle (\r)
            (0.5, 1.2) circle (\r)
            (0.5, 2.1) circle (\r)
            (-0.5, 1.1) circle (\r)
            (-0.7, 1.9) circle (\r)
        ; 
    }

    \newcommand{\drawClusterTwo}[1]{
        \draw[fill=#1]
            (1.4, 0.4) circle (\r)
            (1.6, 1) circle (\r)
            (1.3, 1) circle (\r)
            (2.0, 0.2) circle (\r)
            (2.0, 0.8) circle (\r)
            (1.0, 0.1) circle (\r)
            (0.8, 0.6) circle (\r)
        ; %
    }    

    \newcommand{\drawDataset}[2]{
        % Axis
        \draw [<->] 
            (0, 3) node [above] {$f_2$} |- 
            (2, 0) node [below] {$f_1$}
        ;
        \drawClusterOne{#1}
        \drawClusterTwo{#2}
    }

\usepackage{url}
\usepackage[colorlinks]{hyperref}
\usepackage{cleveref}